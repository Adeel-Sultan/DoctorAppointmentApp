<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adeelsultan.phcproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".LoginAsPatient" />
        <activity android:name=".LoginAsDoctor" />
        <activity android:name=".SignupAsPatient" />
        <activity android:name=".SignupAsDoctor" />
        <activity android:name=".DashboardActivity" android:label="@string/title_activity_dashboard" android:theme="@style/AppTheme" />
        <activity android:name=".GalleryActivity" />
        <activity android:name=".Dashboard2Activity" />
        <activity android:name=".ComplainActivity" />
        <activity android:name=".ComplaintToPatientActivity"
            android:parentActivityName=".Dashboard2Activity"/>
    </application>

</manifest>